# API 路由重写规则
# 将 /api/v1/* 请求转发到 api/v1/index.php

RewriteEngine On

# CORS 预检请求直接返回
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule .* - [R=204,L]

# 非静态文件请求转发到 index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^v1/(.*)$ v1/index.php [L,QSA]
