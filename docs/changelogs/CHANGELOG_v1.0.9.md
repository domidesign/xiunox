# v1.0.9 - 2026-07-10

> **版本说明**: 后台入口与会话管理优化 —— 管理员令牌过期改为 flash cookie + HX-Redirect 友好跳转，插件语言包兜底加载，前台新增用户等级页

## 🔧 重构与优化

### 后台入口优化
- **令牌过期处理**（`86c5e73`）：管理员登录态过期时，改用 flash cookie 传递 toast 提示 + 跳转登录页；HTMX 请求通过 `HX-Redirect` 头整页跳转，普通请求 303 重定向，替代原来的整页 message.htm
- **插件后台语言包兜底加载**（`e7a6d3e`）：修复插件 `bbs_admin.php` 语言包因 hook 语法冲突无法加载的问题

### 缓存与会话
- **CacheHelper 优化**：缓存处理逻辑调整
- **会话管理优化**（`model/session.func.php`）：会话生命周期与清理改进

### 权限与前台
- **版块权限补充**（`model/forum_access.func.php`）：版块访问/发帖权限判断完善
- **新增用户等级页**（`view/htm/my_level.htm`）：展示用户等级与升级进度
- **前台多模块优化**：主题/帖子/用户/积分/通知路由，header/sidebar 模板，JS 脚本同步更新
- **后台多模块优化**：设置/审核/版块/用户管理菜单与页面

## 📊 统计
- 文件总数：85（新增 1，修改 84，删除 0）
- 代码量：+929 行 / -303 行
- 提交范围：`f4201b8` → `e7a6d3e`（2 个提交）
