# 项目规则（每次对话开始必须读取并遵守）

## 推送后流程
每次 `git push` 完成后，必须立即：
1. 输出本次推送的修改文件列表（新增/修改/删除分类）
2. 输出更新日志（按 `update_YYYYMMDD.md` 格式写入项目根目录，简洁描述，不含具体代码）
3. 提交并推送更新日志文件

## 其他约定
- 遵循 `update_YYYYMMDD.md` 命名规范，同一天追加内容
- 更新日志按功能模块分段：API / 服务层 / 后台 / 前端 / 多语言 / 其他
- 提交信息使用中文，遵循 conventional commits 风格
- pre-commit 钩子要求 index.php 的 DEBUG 为 0
